apiVersion: v1
kind: Pod
metadata:
  name: frontend
  namespace: network-demo
  labels:
    app: frontend
    role: web
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    ports:
    - containerPort: 80
